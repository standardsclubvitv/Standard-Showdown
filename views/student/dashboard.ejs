<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="icon" type="image/png" href="https://standardsclubvitv.github.io/image-api/images/logo.png">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Standards | Dashboard</title>
  <style>
    :root {
      --primary: #3B82F6;
      --primary-dark: #1D4ED8;
      --primary-light: #93C5FD;
      --secondary: #34D399;
      --success: #10B981;
      --warning: #F59E0B;
      --danger: #EF4444;
      --text-dark: #1F2937;
      --text-light: #F9FAFB;
      --text-muted: #6B7280;
      --background: #F3F4F6;
      --card-bg: #FFFFFF;
      --border: #E5E7EB;
      --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
      --table-header: #F9FAFB;
      --table-row-alt: #F3F4F6;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      background-color: var(--background);
      color: var(--text-dark);
      line-height: 1.6;
      min-height: 100vh;
      padding: 0;
    }
    
    .dashboard {
      display: flex;
      min-height: 100vh;
    }
    
    .sidebar {
      width: 250px;
      background-color: var(--text-dark);
      color: var(--text-light);
      padding: 2rem 1rem;
      position: fixed;
      height: 100vh;
      overflow-y: auto;
    }
    
    .sidebar-logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--primary-light);
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .sidebar-nav {
      list-style: none;
    }
    
    .sidebar-nav-item {
      margin-bottom: 0.5rem;
    }
    
    .sidebar-nav-link {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      color: var(--text-light);
      text-decoration: none;
      border-radius: 0.5rem;
      transition: background-color 0.3s ease;
    }
    
    .sidebar-nav-link:hover,
    .sidebar-nav-link.active {
      background-color: rgba(255, 255, 255, 0.1);
    }
    
    .main-content {
      flex: 1;
      margin-left: 250px;
      padding: 2rem;
    }
    
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid var(--border);
    }
    
    .welcome-message {
      font-size: 1.5rem;
      font-weight: 600;
    }
    
    .logout-form {
      display: inline-block;
    }
    
    .logout-button {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background-color: transparent;
      color: var(--text-dark);
      border: 1px solid var(--border);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.3s ease;
    }
    
    .logout-button:hover {
      background-color: var(--danger);
      color: var(--text-light);
      border-color: var(--danger);
    }
    
    .section-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      color: var(--text-dark);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .card {
      background-color: var(--card-bg);
      border-radius: 0.75rem;
      box-shadow: var(--shadow);
      padding: 1.5rem;
      margin-bottom: 2rem;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 1.5rem;
    }
    
    th {
      background-color: var(--table-header);
      color: var(--text-dark);
      font-weight: 600;
      text-align: left;
      padding: 0.75rem 1rem;
      border-bottom: 2px solid var(--border);
    }
    
    td {
      padding: 0.75rem 1rem;
      border-bottom: 1px solid var(--border);
    }
    
    tr:nth-child(even) {
      background-color: var(--table-row-alt);
    }
    
    .quiz-title {
      font-weight: 500;
      color: var(--text-dark);
    }
    
    .quiz-date {
      color: var(--text-muted);
      font-size: 0.875rem;
    }
    
    .quiz-action {
      text-align: center;
    }
    
    .take-quiz-link {
      display: inline-block;
      background-color: var(--primary);
      color: var(--text-light);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 500;
      transition: background-color 0.3s ease;
    }
    
    .take-quiz-link:hover {
      background-color: var(--primary-dark);
    }
    
    .submitted-badge {
      display: inline-block;
      background-color: var(--success);
      color: var(--text-light);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-size: 0.875rem;
      font-weight: 500;
    }
    
    .empty-state {
      text-align: center;
      padding: 3rem 1rem;
      color: var(--text-muted);
    }
    
    .empty-state-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--border);
    }
    
    .empty-state-text {
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }
    
    @media (max-width: 1024px) {
      .sidebar {
        width: 200px;
      }
      
      .main-content {
        margin-left: 200px;
      }
    }
    
    @media (max-width: 768px) {
      .dashboard {
        flex-direction: column;
      }
      
      .sidebar {
        position: static;
        width: 100%;
        height: auto;
        padding: 1rem;
      }
      
      .main-content {
        margin-left: 0;
        padding: 1.5rem;
      }
      
      .header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }
      
      .logout-form {
        align-self: flex-end;
      }
    }
  </style>
  <!-- Including Font Awesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
</head>
<body>
  <div class="dashboard">
    <div class="sidebar">
      <div class="sidebar-logo">
        <i class="fas fa-graduation-cap"></i>
        <span>Standards VITv</span>
      </div>
      <ul class="sidebar-nav">
        <li class="sidebar-nav-item">
          <a href="/student/dashboard" class="sidebar-nav-link active">
            <i class="fas fa-home"></i>
            <span>Dashboard</span>
          </a>
        </li>
      </ul>
    </div>
    
    <div class="main-content">
      <div class="header">
        <h2 class="welcome-message">Welcome, <%= student.name %></h2>
        <form action="/student/logout" method="POST" class="logout-form">
          <button type="submit" class="logout-button">
            <i class="fas fa-sign-out-alt"></i>
            <span>Logout</span>
          </button>
        </form>
      </div>
      
      <div class="card">
        <h3 class="section-title">
          <i class="fas fa-clipboard-list"></i>
          <span>Your Available Quizzes</span>
        </h3>
        
        <% if(quizzes && quizzes.length > 0) { %>
          <table>
            <tr>
              <th>Quiz Title</th>
              <th>Date</th>
              <th>Action</th>
            </tr>
            <% quizzes.forEach(quiz => { %>
              <tr>
                <td class="quiz-title"><%= quiz.title %></td>
                <td class="quiz-date"><%= new Date(quiz.date).toLocaleDateString() %></td>
                <td class="quiz-action">
                  <% if (student.responses.includes(quiz._id)) { %>
                    <span class="submitted-badge">
                      <i class="fas fa-check-circle"></i> Completed
                    </span>
                  <% } else { %>
                    <a href="/student/quiz/<%= quiz._id %>" class="take-quiz-link">
                      <i class="fas fa-play-circle"></i> Take Quiz
                    </a>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </table>
        <% } else { %>
          <div class="empty-state">
            <div class="empty-state-icon">
              <i class="far fa-clipboard"></i>
            </div>
            <p class="empty-state-text">No quizzes are currently available</p>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
